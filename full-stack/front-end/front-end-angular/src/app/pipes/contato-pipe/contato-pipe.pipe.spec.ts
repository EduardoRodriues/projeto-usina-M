import { ContatoPipePipe } from './contato-pipe.pipe';

describe('ContatoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContatoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
